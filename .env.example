# SPDX-License-Identifier: Apache-2.0
# bits-services configuration template. Copy to .env and fill in.
#
# NO secrets belong in .env.example. The signing key is NEVER stored here or
# baked into any image — it is pushed into the running proxy over its ingest
# socket and held only in memory.

# ─── security-proxy (Phase 1) ────────────────────────────────────────────────
# Host directory with the proxy's root-owned config (mounted read-only).
# Put a copy of security-proxy/config.sample.json here as config.json.
SECURITY_PROXY_CONFIG_DIR=
# (The agent + ingest sockets live on the 'signer-sockets' named volume, not a
# host path, so there is no socket dir to configure.)
# Pinned ali-bot commit the proxy image is built from (recorded; the actual pin
# is the submodule gitlink).
ALIBOT_REF=

# ─── bits-console backend (Stage B) ──────────────────────────────────────────
# Security-proxy sign route the backend calls (B3). On the same node this is the
# proxy's advertised address; empty is fine for the B1 skeleton.
BITS_SIGN_PROXY_URL=
# GitLab API base for identity/authorization (B2), e.g. https://gitlab.cern.ch/api/v4
GITLAB_API_URL=
# GitLab OIDC confidential-client id (B2); the client SECRET goes in .env, not here.
# BITS_CONSOLE_OIDC_CLIENT_ID=
# The gate token (BITS_SIGN_PROXY_TOKEN) is provided to the backend at sign time
# from a secure source (co-located token fetch), never stored here.

# ─── monitoring (Phase 4) ────────────────────────────────────────────────────
# (filled when the stack is extracted from cvmfs-testbed)
